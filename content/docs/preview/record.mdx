---
title: 记录工具
description: 记录是 NoteGen 最核心的功能，它由记录工具、标签管理、记录内容组成。
icon: Highlighter
---

import { ScanText, ImagePlus, FilePlus, CopySlash, Link } from 'lucide-react'
import Image from 'next/image'

# 记录

位于记录页面左侧。

<Image width={600} height={400} src="https://s2.loli.net/2025/05/23/7TpehH9ZAOF8Cuw.png" alt="记录工具界面显示记录选项" />

## 记录工具

记录工具提供了`截图`、`插图`、`文件`、`文本`、`链接` 5 种记录方式，合理利用，可以针对不同场景提高记录效率。

<Cards>
  <Card icon={<CopySlash />} title="文本记录">
    直接记录文本，适合大段文本记录或临时记录。
  </Card>
  <Card icon={<ScanText />} title="截图记录">
    从窗口截图中快速识别和记录其内容。
  </Card>
  <Card icon={<ImagePlus />} title="插图记录">
    可进行图片记录，整理时将图片插入到文章中。
  </Card>
  <Card icon={<FilePlus />} title="文件记录">
    从本地选择文件，支持文本和 PDF 格式，PDF 自动识别文字。
  </Card>
  <Card icon={<Link />} title="链接记录">
    输入链接，进行网络内容爬取。
  </Card>
</Cards>

### 截图记录

<Image width={600} height={400} src="https://s2.loli.net/2025/05/29/QsPSBFw4qIHNgtl.png" alt="截图记录界面显示区域选择" />

从窗口截图中框选区域，将框选区域截图，通过 OCR 识别文字，再由 AI 生成记录描述（方便用户分别内容），进行图床上传。

- 窗口截图顺序根据当前活动窗口的 Z 值排序。
- 框选结束后，双击框选区域完成截图。
- 最小化的窗口无法被截图。

### 插图记录

从本地选择图片或 AI 助手识别到剪贴板图片时，可进行图片记录，运行逻辑与截图记录一致。

- `截图记录` 最终以文字的形式记录，将其**识别的文字插入到文章中**，不会插入截图。
- `插图记录` 最终以图片的形式记录，会**以图片的形式插入到文章中**，位置由 AI 通过描述判断。

### 文件记录

从本地选择文件，进行文件记录，支持 `txt`、`md`、`csv`、`pdf` 格式。

- 对于 `txt`、`md`、`csv` 格式，直接以文本形式记录。
- 对于 `pdf` 格式，支持 PDF 文本提取和 OCR 文字识别：
  - 如果 PDF 包含文本层，直接提取文本内容
  - 如果 PDF 是扫描版或图片型 PDF，自动使用 OCR 识别文字（基于 Tesseract 引擎）
  - 支持多页 PDF 处理，显示识别进度

### 文本记录

点击后，弹出文本记录弹窗，用户可直接输入文本，适合大段文本记录或临时记录。

### 链接记录

输入链接，进行网络内容爬取，目前不支持 SPA 网站爬取，最终以文本的形式记录。

## 标签管理

标签管理用于管理记录的标签，方便用户对记录进行分类，标签还会影响 AI 助手的对话记录和 AI 的内容关联。

<Image width={600} height={400} src="https://s2.loli.net/2025/05/23/VmhnPKqAlZt5xsk.png" alt="标签管理界面显示标签列表" />

功能列表：

- 点击标签框，弹出标签列表，可进行标签切换。
- 在搜索框输入可进行模糊查询，不存在的标签名，可以创建新标签。
- 右键可进行标签置顶、重命名、删除操作。
- 标签右侧的数字代表该标签下记录的数量。

## 记录内容

记录内容包含了`记录类型`、`记录时间`、`记录描述`、`缩略图`等信息。

<Image width={600} height={400} src="https://s2.loli.net/2025/05/23/bYT1HlExqBWSLh5.png" alt="记录内容显示缩略图和描述" />

功能列表：

- 点击描述，可以弹出记录详情。
- 点击缩略图可以预览大图。
- 右键可弹出菜单，提供了大量方便的工具，例如转移标签、复制链接和记录的管理等。
